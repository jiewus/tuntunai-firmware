ESP-IDF 5.5.4 已验证可用，项目已经完整编译成功，可以烧录。

生成的固件：

- [xiaozhi.bin](/Users/jiewus/projects/J/TuntunLife/ESP32-C5.Firmware/build/xiaozhi.bin)：2.5 MB
- [merged-binary.bin](/Users/jiewus/projects/J/TuntunLife/ESP32-C5.Firmware/build/merged-binary.bin)：9.4 MB，完整合并固件
- 应用分区剩余约 37%

每次打开新终端（需要先 cd 到当前固件相关所在的文件夹内）先执行：

```bash
source /Users/jiewus/.espressif/v5.5.4/esp-idf/export.sh
```

以后编译：

```bash
idf.py fullclean
idf.py build
```

当前检测到设备串口为 `/dev/cu.usbmodem83101`，烧录并查看日志：

```bash
idf.py -p /dev/cu.usbmodem83101 flash monitor
```

退出日志监视器按 `Ctrl+]`。

我还恢复了必要的 `78/esp-ml307` 网络抽象依赖，并更新了 [release.py](/Users/jiewus/projects/J/TuntunLife/ESP32-C5.Firmware/scripts/release.py)，现在它能识别你的 ESP-IDF 5.5.4 安装位置。
